# Caddy обеспечивает бесплатный HTTPS и проксирует запросы к агенту.
# Домен через sslip.io указывает на IP сервера автоматически (без покупки домена).
# Если сменишь сервер/IP — поменяй домен ниже на <новый-ip>.sslip.io
147.45.178.223.sslip.io {
    reverse_proxy 127.0.0.1:8000
}
